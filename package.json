{
  "name": "dockerbar-linux",
  "version": "0.1.0",
  "description": "Docker container health monitor for DankBar",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
